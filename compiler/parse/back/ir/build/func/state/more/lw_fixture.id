// An open lowering state, reusing ir_rows (../../../../core/fixture/fn0.id):
// f0 already has one block from ir_rows itself, so opening it adds a second
// entry/body pair -- block 2 (entry) and block 3 (body, the current block).

lw_fixture() {
  ir_rows();
  int ret_i = lw_open(0);
} return void;
():() then check_failed:(0)
():() then init_units:(0)

// The same, opening fn 1 (fn1id) instead of fn 0.
lw_fixture2() {
  ir_rows();
  int ret_i = lw_open(1);
} return void;
():() then check_failed:(0)
():() then init_units:(0)

chk_lwcur() {
  int ret_i = (import lwcur)[0];
} return int ret_i;
given lw_fixture ():(3)
given lw_fixture2 ():(3)
