// The first of the four shapes: whether the operator is a comparison at all.
lw_bin_op(int node, string op) {
  int v = 0 - 1;
  if(lw_ispred(op) == 1) {
    v = lw_cmp(node);
  } else {
    v = lb2(node);
  }
} return int v;
given str_fixture2 ((import cmp_node), "<"):(6)
given lw_fix_bin ((import fix_bin_n), "+"):(2)
