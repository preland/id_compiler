// init_opt (../../cell.id) returns void by its own effects, not its value
// (always 0), so this is a void wrapper a case can `given`, and a reader for
// the state it left -- for init_opt's own two cases.

optfx_setup() {
  env_init();
  int r = init_opt();
} return void;
():() then check_failed:(0)
():() then pra_len_check:(0)

pra_len_check() {
  int n = len((import pra));
} return int n;
given optfx_setup ():(0)
given optfx_setup ():(0) then check_failed:(0)

// The other table m2r_row pushes a row onto, read as pra_len_check reads
// pra. Filed here rather than beside its export in ../../cell.id, which this
// leaves at its three-function cap.
outv_len() {
  int n = len((import outv));
} return int n;
given optfx_setup ():(0)
given m2r_row_fixture ():(1)
