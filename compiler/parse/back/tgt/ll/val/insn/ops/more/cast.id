
ll_iscast(string op) {
  int r = 0;
  if(op == "zext" || op == "sext" || op == "trunc" || op == "sitofp" || op == "fptosi" || op == "ptrtoint" || op == "inttoptr") {
    r = 1;
  }
} return int r;
("zext"):(1)
("add"):(0)

ll_negop(string t) {
  string s = "sub";
  if(ty_kind(t) == "float") {
    s = "fsub";
  }
} return string s;
given env_init ("float"):("fsub")
given env_init ("int"):("sub")

ll_phis(int id) {
  string s = "";
  int i = 0;
  while(i < ir_l_count(id)) {
    int[] ir_l_v = ir_l(id);
    s = s + ll_sep(i) + "[ " + ll_val(ir_l_v[i]) + ", %b" + ir_bl(id)[i] + " ]";
    i = i + 1;
  }
} return string s;
given phi_fixture ((import ir_phi)):("[ %v0, %b7 ]")
given phi_empty_fixture ((import ir_phi_empty)):("")
