// Two more shapes case/lit/fixture.id's plain ints do not cover: a list
// node (an "arr" wrapping both ints) and the raw int[] of node ids a
// list-taking emitter reads directly, without a wrapping node.

lit_arr_fixture() {
  lit_int_fixture();
  int[] xs = [(import lit_e), (import lit_e2)];
  export int lit_arr_e = newnode("arr", 0, 0, "", "", xs, xs);
} return void;
():() then check_failed:(0)
():() then init_units:(0)

lit_cells_fixture() {
  lit_int_fixture();
  export int[] lit_cells_xs = [(import lit_e), (import lit_e2)];
} return void;
():() then check_failed:(0)
():() then init_units:(0)

// A `(import NAME)` case argument, as the node the parser builds for it
// (front/parse/decl/func/cases/case/given/elem/elem.id): a "cimport" leaf
// naming the export.
lit_import_fixture() {
  env_init();
  export int lit_cimport_e = newleaf("cimport", 0, 0, "gb", "");
} return void;
():() then check_failed:(0)
():() then init_units:(0)
