// casefn/casenode filled in for fixture_ci: fx_target itself, called with
// its own one argument -- (import fixture_zero) -- so emit_case_call
// (case/more/call.id, not new) has what it reads, for ../../query/wrap.id's
// emit_case_call_wrapped.

wrap_fixture() {
  fn_fixture();
  wrap_fixture_case();
} return void;
():() then check_failed:(0)
():() then init_units:(0)

wrap_fixture_case() {
  int ci = (import fixture_ci);
  int[] one = [(import fixture_zero)];
  wrap_fixture_push(ci, one);
} return void;
given fn_fixture ():() then check_failed:(0)
given fn_fixture ():() then init_units:(0)

wrap_fixture_push(int ci, int[] one) {
  int node = newnode("case", 0, 0, "", "", one, one);
  push((import casefn), (import fixture_fn));
  lset((import casenode), ci, node);
} return void;
given fn_fixture (0, [1]):() then check_failed:(0)
given fn_fixture (0, [1]):() then init_units:(0)
