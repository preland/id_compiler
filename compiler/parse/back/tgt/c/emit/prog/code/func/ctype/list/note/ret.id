// A function's return type, noted the same way a case's expected value's is
// (note.id).

// prog = [dup5, dup9] (index 1: dup9), and the real strict fingerprint pass
// over it: dup5/dup9's strict fps differ (they keep the literal's value).
lz_o() {
  lz_n();
  push((import prog), (import lz_fn9));
  uq_fill();
} return void;
():() then check_failed:(0)
():() then init_units:(0)

note_fn_ret(int cfn) {
  if(cfn >= 0) {
    string rt = s2_of(cfn);
    note_ty(rt);
  }
} return void;
given fn_fixture ((import fixture_fn)):()
given fn_fixture (-1):()
