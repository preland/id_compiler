// The base and index types an indexing operation reads twice over (once to
// check the base, once to check the index).

base_type(int id) {
  int i1_of_v = i1_of(id);
  string bt = type_of(i1_of_v);
} return string bt;
given idx_build ((import idx_fix)):("int[]")
given cmp_fixture ((import bin_eq)):("int")

index_type(int id) {
  int i2_of_v = i2_of(id);
  string it = type_of(i2_of_v);
} return string it;
given idx_build ((import idx_fix)):("int")
given cmp_fixture ((import bin_eq)):("int")

bi_mismatch_sys_setup() {
  call2_setup();
  int a = node_int("1", 1, 0);
  export int bi_mismatch_sys_call = node_call("ult", [a]);
} return void;
():() then check_failed:(0)
():() then init_units:(0)
