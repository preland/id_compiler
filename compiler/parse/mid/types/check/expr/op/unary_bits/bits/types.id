// id 14: a bare read of "target" as a value (mid/form/decomp/checks/fwd/'s
// canon_var tags exactly this "fv(target)", never a call's own callee).
farm_j2() {
  farm_j();
  export int f_uvar = node_var("target");
} return void;
():() then check_failed:(0)
():() then init_units:(0)

bits_types(int id, int a, int b) {
  string lt = type_of(a);
  string rt = type_of(b);
  if(ty_pair_known(lt, rt) == 1 && (ty_int(lt) == 0 || ty_int(rt) == 0)) {
    bits_err(id, lt, rt);
  }
} return void;
given cmp_fixture ((import bin_bitand), (import lit_a), (import lit_b)):() then check_failed:(0)
given cmp_fixture ((import bin_bitand), (import lit_str), (import lit_a)):() then check_failed:(1)
