bi_sys_msg(int id) {
  string name = s1_of(id);
  string cw = bi_count_word(name);
  string msg = bi_sys_msg2(id, name, cw);
} return string msg;
given bi_mismatch_sys_setup ((import bi_mismatch_sys_call)):("ult takes exactly 2 arguments, got 1")
given bi_mismatch_sys0_setup ((import bi_mismatch_sys0_call)):("ult takes exactly 2 arguments, got 0")

bi_count_word(string name) {
  int n = bi_arity(name);
  string plural = bi_plural(name);
  string s = n + " argument" + plural;
} return string s;
("ult"):("2 arguments")
("alloc"):("1 argument")

bi_sys_msg2(int id, string name, string cw) {
  int argn_v = flat_arg_count(id);
  string msg = name + " takes exactly " + cw + ", got " + argn_v;
} return string msg;
given bi_mismatch_sys_setup ((import bi_mismatch_sys_call), "ult", "2 arguments"):("ult takes exactly 2 arguments, got 1")
given bi_mismatch_sys_setup ((import bi_mismatch_sys_call), "alloc", "1 argument"):("alloc takes exactly 1 argument, got 1")
