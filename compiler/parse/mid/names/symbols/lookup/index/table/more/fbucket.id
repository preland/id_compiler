// Split from push_buckets so that block stays within the action limit.
push_fbucket() {
  int[] new_bucket_v2 = new_bucket();
  push((import fidx), new_bucket_v2);
} return void;

regrow_a() {
  env_init();
  init_reg();
  push((import dvfunc), "f");
} return void;
():() then check_failed:(0)
():() then init_units:(0)
