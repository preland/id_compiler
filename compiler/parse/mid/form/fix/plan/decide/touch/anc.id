// Whether an event after i, up to hi, is flagged in fl and contains i.
fix_anc(int[] fl, int i, int hi) {
  int j = i + 1;
  int ok = 0;
  while(j <= hi) {
    ok = fix_anc_at(fl, i, j, ok);
    j = j + 1;
  }
} return int ok;
given fix_touch_fixture ([0, 1], 0, 1):(1)
given fix_touch_fixture ([0, 0], 0, 1):(0)

fix_anc_at(int[] fl, int i, int j, int ok) {
  int r = ok;
  if(fl[j] == 1 && (import fixe_lo)[j] <= i) {
    r = 1;
  }
} return int r;
given fix_touch_fixture ([0, 1], 0, 1, 0):(1)
given fix_touch_fixture ([0, 0], 0, 1, 0):(0)

fix_evtype(int i) {
  string t = (import fixe_nt)[i];
  if(t == "") {
    t = type_of((import fixe_n)[i]);
  }
} return string t;
given fix_touch_fixture (0):("int")
given fix_touch_fixture (1):("word")
