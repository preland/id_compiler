//   fixe_sep  1 for a moved event that gets a declaration of its own
//   fixe_nm   the name it is given
//   fixe_an   set only for the node at the top of a call's argument: the
//             whole-argument name to use if it is named as one (out/emit/).
fix_ev_reset4() {
  export int[] fixe_sep = [];
  export string[] fixe_nm = [];
  fix_ev_reset5();
} return void;
given fix_fixture ():() then calls fix_ev_reset5:[1]
given fix_touch_fixture ():() then calls fix_ev_reset5:[1]

//   fixe_ru  1 for a call event whose name is reused from an earlier
//            identical, still-trustworthy call (out/emit/name/pick/reuse/):
//            its text is still replaced by the name, but it gets no
//            declaration of its own -- the reused name's already has one.
fix_ev_reset5() {
  export string[] fixe_an = [];
  export int[] fixe_ru = [];
} return void;
given fix_fixture ():() then check_failed:(0)
given fix_fixture ():() then init_units:(0)

// How many events have been marked for a declaration of their own, and how
// many have been named. A `then` check takes no parameters, so a function
// whose whole effect is a push or an lset on one of these columns can only be
// observed through a reader of it -- fix_sep_at and fix_mark_sep
// (../../../decide/touch/sep.id) are what this one is for.
fixe_sep_count() {
  int n = 0;
  int i = 0;
  while(i < len((import fixe_sep))) {
    n = n + (import fixe_sep)[i];
    i = i + 1;
  }
} return int n;
given fix_fixture ():(0)
given fix_touch_fixture ():(0)
