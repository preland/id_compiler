fix_pick_name(int i, string t) {
  string an = (import fixe_an)[i];
  string name = fix_pick_name2(i, t, an);
} return string name;
given fix_pick_fixture_an0 (0, "int"):("lit_v")
given fix_pick_fixture_an_named (0, "int"):("myarg")

// fixe_an[0] empty: fix_pick_name2 takes the plain, fix_stem branch.
fix_pick_fixture_an0() {
  fix_pick_fixture_n();
  push((import fixe_an), "");
} return void;
():() then check_failed:(0)
():() then init_units:(0)

// fixe_an[0] set, and fixe_cl[0] forced (fix_pick_fixture_named): fix_pick_name2
// takes the whole-argument, fix_choose_arg branch.
fix_pick_fixture_an_named() {
  fix_pick_fixture_named();
  push((import fixe_an), "myarg");
} return void;
():() then check_failed:(0)
():() then init_units:(0)
