// A func node whose declared return type narrows the value it hands back:
// `int` declared, a word-sized literal returned. The pair ../narrow.id's
// fix_ret_narrows and fix_ret_needs (and, by the same declared/value split,
// ../../../../../types/check/expr/more/want/ret/narrow.id's chk_ret_narrow
// and ret_narrow_err) all judge as narrowing.

ret_narrows_val() {
  env_init();
  int e = node_int("99999", 99999, 1);
  export int narrow_e = e;
} return void;
():() then check_failed:(0)
():() then init_units:(0)

ret_narrows_fn() {
  ret_narrows_val();
  int[] none = [];
  export int narrow_f = node_func("f", "int", none, none, (import narrow_e));
} return void;
():() then check_failed:(0)
():() then init_units:(0)
