// Whether tokens b..e lie inside a range this run replaces, checked in
// fix_in_moved's chain (../../paren/more.id) once its own block is full.
fix_within_at(int i, int b, int e, int ok) {
  int r = ok;
  if((import fixd_lo)[i] <= b && e <= (import fixd_hi)[i]) {
    r = 1;
  }
} return int r;
