// bump_v is already declared in this function.
go(int[] c) {
  int bump_v = 3;
  int x = twice(bump(c)) + bump_v;
} return int x;

// bump_v2 is a function, so it is not free either.
bump_v2(int k) {
  int j = k + 1;
} return int j;

run(int[] c) {
  print(twice(bump(c)));
} return void;
