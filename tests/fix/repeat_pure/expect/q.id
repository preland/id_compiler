// inf_pow2 writes nothing, so two identical calls share one name and one
// evaluation instead of running twice.
use1(int log) {
  int inf_pow2_v = inf_pow2(log);
  int r = combine(inf_pow2_v, inf_pow2_v);
} return int r;
