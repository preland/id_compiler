bump(int[] c) {
  c[0] = c[0] + 1;
  int n = c[0];
} return int n;

twice(int n) {
  int m = n * 2;
} return int m;

loop(int[] c) {
  while(bump(c) < twice(bump(c))) {
    print("again");
  }
} return void;
