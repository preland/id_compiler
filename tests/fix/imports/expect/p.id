// tab_init declares the export, so a call that can run it can change what
// (import tab) reads.
tab_init() {
  export int[] tab = [0, 0];
} return void;

bump(int[] c) {
  c[0] = c[0] + 1;
  int n = c[0];
} return int n;

twice(int n) {
  int m = n * 2;
} return int m;
