# How each project in this repo stands against the REAL ../idstd.
# Read by tests/idstd_real.sh; see that file for what the states mean.
#
#   both       builds with the library and without it
#   needs-std  builds only with it
#   broken     builds only WITHOUT it -- a collision with the library
#   neither    builds neither way (needs --backend or flags)
#
# Every `broken` line is work, not a permitted state: C9 in docs/IDSTD.md.
# Measured 2026-08-19.

adventure        both
average          needs-std
calc             both
control          both
fsdemo           needs-std
hello            both
idc_in_id_calc   needs-std
idview           needs-std
wordcount        needs-std

# Graphics demos: gfx and gl are the standard library's, so they build only
# with it. So does fsdemo, above, for fs.
flyover          needs-std
fpsmaze          needs-std
galaxy           needs-std
gfxdemo          needs-std
gl3d             needs-std
gl3dgame         needs-std

# ---------------------------------------------------------------------------
# The compiler's own two stages, PORTED. They each carried a private copy of
# idstd's `lset`; both now call the library's. That is the rule working -- the
# function existed in a dependency, so the program should not define it again --
# and it means id_development needs an idstd checkout to build its own
# compiler. Deliberate; see bin/idc's bootstrap comment.
compiler/lex     needs-std
compiler/parse   needs-std

# ---------------------------------------------------------------------------
# moonbuggy, solitaire -- the two terminal games. The engine they each bundled
#   (and demos/engine, its reference copy) is idstd's sys/io/term now.
moonbuggy        needs-std
solitaire        needs-std
