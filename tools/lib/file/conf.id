// The file tree's constant. fs_open, fs_read and fs_close are the standard
// library's fs backend, linked where they are reached.

// Cells per fs_read. One buffer of this size serves a whole read.
int file_chunk = 65536;
