// Combining handles, and loading one onto a GL matrix stack. See gl.h.

native gl_mat_mul(int a, int b) return int;

native gl_set_projection(int matrix) return int;

native gl_set_modelview(int matrix) return int;
